import styled from "styled-components";

export const AddToBalanceWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 1em;
`;
export const StyledForm = styled.form`
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 1em;
  input {
    font-size: 1.3em;
    padding: 0.5em;
  }
`;
